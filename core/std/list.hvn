module List

data List a = nil | cons a (List a)

head (cons x _) = x

tail (cons _ xs) = xs

length nil = zero
length (cons _ xs) = succ (length xs)

append nil ys = ys
append (cons x xs) ys = cons x (append xs ys)
