module Result

data Result

ok : a -> Result
err : e -> Result

is_ok (ok _) = true
is_ok (err _) = false

is_err (err _) = true
is_err (ok _) = false

map_result f (ok x) =
    ok (f x)

map_result f (err e) =
    err e
