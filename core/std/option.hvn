module Option

data Option

none : Option
some : a -> Option

is_some (some _) = true
is_some none = false

is_none none = true
is_none (some _) = false

map_option f none =
    none

map_option f (some x) =
    some (f x)
